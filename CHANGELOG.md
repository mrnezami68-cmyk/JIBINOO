# 📒 چنج‌لاگ پروژه جیبینو (JIBINOO)

این فایل تاریخچه کامل تغییرات پروژه را **فاز به فاز** ثبت می‌کند: **چه چیزی** تغییر کرده، **چرا**، **کی**، **چگونه** (توضیح فنی) و اینکه **آیا خطایی حین کار رخ داده** و چگونه اصلاح شده است.
این فایل در پایان هر فاز به‌روزرسانی می‌شود و مرجع اصلی پیگیری کار است.

- شاخه کاری: `arena/01a0d5c2-jibinoo`
- مبدأ کار: بازبینی کامل کد و فهرست ۷ باگ بحرانی «یکپارچگی داده‌های مالی»
- تاریخ شروع: ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

---

## فهرست فازها

| فاز | عنوان | وضعیت |
|---|---|---|
| ۰ | آماده‌سازی، نقشه فراخوانی‌ها، کشف باگ پنهان | ✅ انجام شد |
| ۱ | زیرساخت: موتور نقد اتمیک در `types.ts` / `store.tsx` | ✅ انجام شد |
| ۲ | اصلاح موتور تحلیل (`analysis.ts`) | ✅ انجام شد |
| ۳ | اصلاح مودال تراکنش (`TxModal.tsx`) | ✅ انجام شد |
| ۴ | اصلاح صفحات (وام / اهداف / دارایی / خوش‌آمد) | ✅ انجام شد |
| ۵ | برچسب‌های دفتر تراکنش و متن‌های حذف امن | ✅ انجام شد |
| ۶ | بهداشت کد (رفع خطاهای lint از نوع متغیر بلااستفاده) | ✅ انجام شد |
| ۷ | تأیید نهایی (tsc / build / lint)، کامیت و گزارش | ✅ انجام شد |
| ۸ | کشف و پایش منابع قیمت (بیت‌پین، TGJU/طلایار، Gold-API، COMEX، CoinGecko) | ✅ انجام شد |
| ۹ | موتور قیمت چندمنبعه + دارایی‌های جدید (سکه‌ها، آب‌شده، نقره، مس، پلاتین) | ✅ انجام شد |
| ۱۰ | سند راهکار قیمت (`docs/PRICE_ENGINE.md`)، تأیید نهایی و انتشار | ✅ انجام شد |

---

## فهرست باگ‌های در حال اصلاح (بحرانی — یکپارچگی داده)

| # | باگ | فایل | فاز رفع |
|---|---|---|---|
| ۱ | پرداخت قسط از صفحه وام → موجودی **دو بار** کم می‌شد (`payLoan` + `addTx`) | `Loans.tsx` | ۱ و ۴ |
| ۲ | «انتقال به هدف» از مودال تراکنش → پول از نقد می‌رفت ولی به هدف اضافه نمی‌شد | `TxModal.tsx` | ۳ |
| ۳ | «پرداخت قسط» از مودال تراکنش → در `loan.payments` ثبت نمی‌شد | `TxModal.tsx` | ۳ |
| ۴ | انتقال به هدف از صفحه اهداف → در دفتر تراکنش‌ها ثبت نمی‌شد (برخلاف متن UI) | `Goals.tsx` / `store.tsx` | ۱ و ۴ |
| ۵ | فروش دارایی → هیچ تراکنشی در دفتر ثبت نمی‌شد (مسیر `sell` کد مرده بود) | `Assets.tsx` / `store.tsx` | ۱ و ۴ |
| ۶ | ثبت وام → اصل مبلغ وام به موجودی نقد واریز نمی‌شد (ارزش خالص از روز اول اشتباه) | `Loans.tsx` / `store.tsx` | ۱ و ۴ |
| ۷ | حذف تراکنش → فقط اثر نقدی برمی‌گشت نه `payments/transfers/quantity` | `store.tsx` | ۱ |
| ۸ ⚡ | **کشف‌شده حین اجرا:** موجودی اولیه در آنبوردینگ ذخیره نمی‌شد و گم می‌شد | `Welcome.tsx` / `store.tsx` | ۱ و ۴ |

> ⚡ باگ شماره ۸ در فهرست اولیه ۷ موردی نبود؛ حین نقشه‌برداری از فراخوانی‌های «موتور نقد» کشف شد و طبق دستور («اگر خطایی رخداد مشخص شود») اینجا ثبت و همراه بقیه اصلاح می‌شود.

**وضعیت نهایی: هر ۸ باگ ✅ رفع شد — `tsc`، `eslint` (بدون خطای جدید) و `npm run build` سبز است.**

---

## فاز ۰ — آماده‌سازی و نقشه‌برداری ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- ایجاد همین فایل `CHANGELOG.md` به‌عنوان مرجع تغییرات فاز به فاز.
- نقشه کامل همه فراخوانی‌های «موتور نقد» (`addTx`, `deleteTx`, `payLoan`, `transferGoal`, `sellAsset`, `addAsset`, `addLoan`, `delete*`) در کل پروژه.

**چرا:** قبل از هر تغییری باید مشخص می‌شد تغییر پول نقد در چند مسیر پراکنده انجام می‌شود تا هیچ مسیری جا نماند و باگی دوباره برنگردد.

**چگونه:** جستجوی سراسری روی `src/`؛ مشخص شد ۴ مسیر مستقیم تغییر `cash` وجود دارد (`addTx`, `payLoan`, `transferGoal`, `sellAsset`) که ناسازگارند.

**خطاها و اصلاحات:**
- 🔴 **کشف باگ پنهان شماره ۸:** در `Welcome.tsx` مقدار `initialCash` (موجودی اولیه) فقط در UI نمایش داده می‌شد و هنگام پایان آنبوردینگ (`ورود به جیبینو`) هرگز ذخیره نمی‌شد — عملاً اولین داده مالی کاربر گم می‌شد. تصمیم: اضافه شدن اکشن `setCash` به store و فراخوانی هنگام تکمیل آنبوردینگ (فاز ۱ و ۴).
- تصمیم طراحی: برای `vite.config.ts` هیچ تغییری داده نمی‌شود (به ابزار `.vite-source-tags.js` وابسته است؛ خطاهای lint آن عمداً باقی می‌مانند).

---

## فاز ۱ — زیرساخت: موتور نقد اتمیک ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- `src/lib/types.ts`: فیلد `link?: TxLink` به `Tx` اضافه شد؛ تایپ‌های `TxLink` و `AssetSnapshot` ساخته شدند؛ `kind` نوع `loan` مستند شد (`payment` | `principal`).
- `src/lib/store.tsx`:
  - تابع `cashDelta`: **تنها مرجع** محاسبه اثر هر تراکنش روی نقد؛ ثبت و حذف هر دو از آن استفاده می‌کنند تا اثرها دقیقاً آینه هم باشند.
  - `addTx` / `deleteTx`: بازنویسی با `cashDelta`؛ `deleteTx` حالا در صورت وجود `link`، اثر روی رکورد مرتبط را هم برمی‌گرداند (**باگ ۷**): حذف قسط → حذف از `loan.payments`؛ حذف انتقال هدف → حذف از `goal.transfers`؛ حذف سند فروش → بازگرداندن `quantity` (یا بازسازی دارایی از snapshot)؛ حذف سند خرید → کم کردن `quantity`.
  - `payLoan`: اتمیک — **دقیقاً یک بار** کسر نقد + ثبت در `payments` + سند دفتر با پیوند `loan-payment` (**باگ ۱ و ۳**).
  - `transferGoal`: اتمیک — جابه‌جایی نقد + ثبت در `transfers` + سند دفتر با پیوند `goal-transfer` + پذیرش `date`/`title` (**باگ ۲ و ۴**).
  - `sellAsset`: اتمیک — واریز نقد + کسر `quantity` + سند «فروش» با پیوند `asset-sell` و snapshot (**باگ ۵**).
  - `addLoan`: با گزینه `receiveCash` — واریز اصل وام به نقد + سند «دریافت وام» با پیوند `loan-principal` (**باگ ۶**).
  - `addAsset`: با گزینه `fromCash` — خرید از نقد حالا سند خریدِ پیوندخورده می‌سازد.
  - `deleteLoan` / `deleteGoal` / `deleteAsset`: **حذف دوطرفه** — رکورد + همه اسناد پیوندخورده‌اش در دفتر حذف و اثر نقدی آن‌ها برگردانده می‌شود تا تراز همیشه درست بماند.
  - اکشن جدید `setCash` برای موجودی اولیه آنبوردینگ (**باگ ۸**).

**چرا:** تغییر پول نقد در ۴ مسیر پراکنده و ناسازگار انجام می‌شد؛ هر تراکنشِ خودکار باید هم‌زمان سند دفتر، اثر نقدی و اثر روی رکورد مرتبط (وام/هدف/دارایی) را در **یک به‌روزرسانی اتمیک** انجام دهد و حذف آن دقیقاً آینه‌اش باشد.

**چگونه:** هر عملیاتِ مالی به یک reducer اتمیک در store تبدیل شد؛ سند دفتر با `TxLink` به رکورد مرتبط پیوند می‌خورد تا `deleteTx` بتواند برگشت کامل بدهد. مهاجرت داده قدیمی لازم نیست: فیلد `link` اختیاری است و اسناد قدیمیِ بدون پیوند مثل قبل (فقط برگشت نقدی) رفتار می‌کنند.

**خطاها و اصلاحات:**
- ⚠️ یکی از ویرایش‌ها (افزودن `setCash` به لیست `value` در `useMemo`) به‌دلیل ناهمخوانی الگو اعمال نشد (بقیه اعمال شدند). **تشخیص:** اجرای `tsc` بعد از ویرایش‌ها. **اصلاح:** بازخوانی بخش `useMemo` و درج `setCash` در لیست `value` (لیست `deps` در همان مرحله درست شده بود). پس از اصلاح، `tsc -b` کاملاً سبز شد.
- تصمیم طراحی (ثبت‌شده): حذف سند «دریافت وام» فقط اثر نقدی را برمی‌گرداند و خود وام در بخش وام‌ها می‌ماند (از آنجا با حذف وام، کل اسناد مرتبط پاک می‌شوند). در `deleteTx` مستند شده است.

---

## فاز ۲ — اصلاح موتور تحلیل (`analysis.ts`) ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:** در `summarize` فیلد `loanPaid` دیگر «دریافت اصل وام» (`kind: 'principal'`) را جمع نمی‌زند؛ فیلد جدید `loanReceived` برای اصل وام دریافتی اضافه شد.

**چرا:** بعد از باگ ۶، سند «دریافت وام» هم وارد دفتر شد؛ بدون این اصلاح، وارد شدن پول وام در «اقساط پرداختی»، «نرخ فشار بدهی» (`financialHealth`) و گزارش‌های ماهانه عدد می‌ساخت.

**چگونه:** فیلتر `t.type === 'loan' && t.kind !== 'principal'` برای `loanPaid` و جمع جدید `loanReceived` در `PeriodSummary`.

---

## فاز ۳ — اصلاح مودال تراکنش (`TxModal.tsx`) ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- نوع **«انتقال به هدف»** دیگر با `addTx` سند خام نمی‌سازد؛ مستقیم به `transferGoal` (اکشن اتمیک) واگذار شد (**باگ ۲**).
- نوع **«پرداخت قسط»** دیگر با `addTx` سند خام نمی‌سازد؛ مستقیم به `payLoan` (اکشن اتمیک) واگذار شد (**باگ ۳**).
- بلوک خالی و ناتمام `if (type === 'goal' && goalId) { /* TODO */ }` (علت اصلی باگ ۲) حذف شد.
- اعتبارسنجی جدید: برای انتقال به هدف وجود «هدف» و برای پرداخت قسط وجود «وام» الزامی شد (پیام خطای فارسی).
- انواع درآمد/هزینه/سرمایه‌گذاری مثل قبل با `addTx` ثبت می‌شوند (بدون تغییر رفتار).

**چرا:** مودال تراکنش فقط سند دفتر می‌ساخت و به رکورد مرتبط (هدف/وام) دست نمی‌زد؛ نتیجه: کم شدن نقد بدون افزوده شدن به هدف (ارزش خالص مصنوعی پایین می‌آمد) و پیشرفت‌نکردن وام.

**چگونه:** `submit` به سه شاخه تبدیل شد؛ شاخه‌های هدف/وام فقط اکشن اتمیک store را صدا می‌زنند (عنوان و یادداشت دلخواه کاربر هم منتقل می‌شود).

**خطاها و اصلاحات:**
- 🔧 **تغییر API حین کار:** امضای `payLoan` ابتدا `title?: string` بود؛ برای اینکه «یادداشت» تراکنش در سند خودکار هم حفظ شود، به `opts?: { title?: string; note?: string }` گسترش یافت (و `transferGoal` هم `note` گرفت). این تغییر در همین فاز اعمال و `tsc` سبز شد.

---

## فاز ۴ — اصلاح صفحات (وام / اهداف / دارایی / خوش‌آمد) ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- `Loans.tsx`: حذف `addTx` جداگانه از `onPay` (**باگ ۱ — کسر دوباره نقد**). حالا فقط `payLoan` صدا زده می‌شود. به `AddLoanModal` چک‌باکس «مبلغ وام به موجودی نقد واریز شود» اضافه شد (**باگ ۶**).
- `Assets.tsx`: خرید دارایی از نقد، اتمیک شد (`addAsset(..., { fromCash })`) و سند خریدِ پیوندخورده ساخته می‌شود؛ فراخوانی جداگانه `addTx` حذف شد. فروش دارایی حالا از طریق `sellAsset` اتمیک، سند «فروش دارایی» ثبت می‌کند (**باگ ۵**).
- `Goals.tsx`: انتقال‌ها (که قبلاً فقط در `goal.transfers` ثبت می‌شدند) به‌لطف اکشن اتمیک فاز ۱، حالا سند دفتر هم می‌سازند (**باگ ۴**). متن دیالوگ حذف هدف هم به‌روز شد.
- `Welcome.tsx`: موجودی اولیه (`initialCash`) هنگام «ورود به جیبینو» با اکشن جدید `setCash` ذخیره می‌شود (**باگ ۸ — دیگر گم نمی‌شود**).

**چرا:** صفحات، منطق مالی را بیرون از store تکرار می‌کردند (فراخوانی هم‌زمان `payLoan` + `addTx`، یا `onAdd` + `addTx`) و همین باعث دوباره‌کاری/ناهماهنگی می‌شد. با اتمیک شدن اکشن‌ها، صفحات فقط «فرمان» می‌دهند.

**چگونه:** هر صفحه به اکشن اتمیک مربوطه منتقل شد؛ برای وام چک‌باکس دریافت نقد (پیش‌فرض: فعال) با نمایش موجودی فعلی اضافه شد تا کاربر کنترل داشته باشد.

**خطاها و اصلاحات:**
- ⚠️ ویرایش حذف `Pencil` از ایمپورت `Assets.tsx` اول اعمال نشد (الگوی تک‌خطی نخورد چون ایمپورت چندخطی بود). **تشخیص:** پیام خطای ابزار ویرایش. **اصلاح:** بازخوانی خطوط ایمپورت و حذف سطر `Pencil,` با الگوی چندخطی. پس از آن `tsc` سبز شد.

---

## فاز ۵ — برچسب‌ها و متن‌های حذف امن ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- `Dashboard.tsx` (کامپوننت `TxRow`): سند `loan` با `kind: 'principal'` حالا «دریافت وام» با علامت `+` نمایش داده می‌شود (قبلاً مثل قسط، «پرداخت قسط» با `−` نشان داده می‌شد — گمراه‌کننده).
- متن دیالوگ‌های تأیید حذف در `Loans` / `Goals` / `Assets` / `Transactions` به‌روز شد تا رفتار جدید «حذف دوطرفه / برگشت اثر نقدی» صریح به کاربر اعلام شود.

**چرا:** بعد از ثبت خودکار اسناد جدید (دریافت وام، فروش دارایی، انتقال هدف) برچسب‌ها و متن‌های قدیمی با رفتار واقعی نمی‌خواندند.

---

## فاز ۶ — بهداشت کد (lint) ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:** حذف متغیر/ایمپورت‌های بلااستفاده در `App.tsx`، `Layout.tsx`، `Guide.tsx`، `Profile.tsx`، `Transactions.tsx`، `Assets.tsx`، `Dashboard.tsx`، `Goals.tsx`، `Loans.tsx`. خطاهای lint از **۲۳ به ۸** رسید.

**چرا:** خطاهای `no-unused-vars` ریسک سردرگمی و خطای بعدی می‌آورند؛ این موارد بی‌خطر و مکانیکی بودند.

**تصمیم ثبت‌شده:** ۸ خطای باقی‌مانده عمداً دست‌نخورده ماندند (خارج از دامنه ۷ باگ): `vite.config.ts` (وابسته به ابزار `.vite-source-tags.js`)، قواعد `react-hooks` در `Layout.tsx`/`charts.tsx`، تایپ `any` در `prices.ts`، قانون `react-refresh` در `store.tsx`/`ui.tsx`. برای فاز آینده «بهداشت کد» پیشنهاد شده‌اند.

---

## فاز ۷ — تأیید نهایی، کامیت و گزارش ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**نتایج تأیید:**
- `tsc -b` ✅ (در پایان هر فاز هم اجرا شد — همه سبز)
- `npm run build` ✅ (فقط هشدار قدیمی حجم چانک > ۵۰۰KB — همان باگ شماره ۱۷ گزارش بازبینی؛ مربوط به این فاز نیست)
- `eslint .` ✅ بدون هیچ خطای جدید (۸ خطای قدیمیِ مستندشده در فاز ۶)

**خطاها و اصلاحات حین تأیید:**
- ⚠️ **دو خطای lint جدید ناشی از تغییرات خودمان** پیدا شدند: ۱) خروجی `export` تابع `cashDelta` با قانون `react-refresh/only-export-components` تداخل داشت؛ ۲) در `deleteTx` متغیر `cash` با `let` تعریف شده بود در حالی که دیگر بازتعریف نمی‌شد (`prefer-const`). **اصلاح فوری:** `cashDelta` خصوصی ماژول شد و `let` → `const`. lint نهایی: ۸ خطا (همگی قدیمی).

**چک‌لیست تست دستی پیشنهادی (QA):**
1. ثبت قسط از صفحه وام ← موجودی فقط یک بار کم شود و در دفتر، سند «پرداخت قسط» با `−` باشد.
2. ثبت «انتقال به هدف» از مودال تراکنش ← هم موجودی کم و هم درصد هدف زیاد شود (ارزش خالص ثابت بماند).
3. ثبت وام با چک‌باکس فعال ← موجودی به اندازه وام زیاد و سند «دریافت وام» با `+` ثبت شود؛ ارزش خالص ثابت بماند.
4. فروش دارایی ← سند «فروش دارایی» در دفتر ثبت شود.
5. حذف هر سند پیوندخورده از دفتر ← رکورد مرتبط (وام/هدف/دارایی) هم به حالت قبل برگردد.
6. تکمیل آنبوردینگ با موجودی اولیه ← عدد واردشده به‌عنوان موجودی نقد بماند.

**مرتبط:** خلاصه کار در گفتگو ارائه شد؛ این فایل مرجع دائمی «چه / چرا / کی / چگونه / خطاهای احتمالی» است و باید در هر مرحله بعدی هم به‌روز شود.

---
---

# 📌 فازهای مرحله دوم — «قیمت‌ها به‌روز نمی‌شوند» (درخواست کاربر)

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵) — گزارش کاربر: قیمت دلار و طلا ثابت می‌مانند؛ دارایی‌هایی
مثل سکه بهار/ربع/نیم‌سکه، طلای آب‌شده، نقره گرمی، مس و پلاتین باید در دارایی‌ها باشند؛
از رویکرد «دیدبان هوشمند بازار» (استخراج چندمنبعه از بیت‌پین/TGJU/کوین‌گکس + قیمت مشتق دلار و ریال) استفاده شود.

---

## فاز ۸ — کشف و پایش منابع قیمت ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:** تست زنده endpointهای عمومی قیمت و تحلیل پاسخ‌ها (بدون تغییر کد).

**چرا:** قبل از بازنویسی موتور باید مشخص می‌شد کدام منابع واقعاً داده زنده می‌دهند، ساختارشان چیست و واحدشان (ریال/تومان/دلار) چیست.

**نتایج کشف (تأییدشده با داده زنده):**
- ✅ **طلایار/دروازه TGJU** (`javadisaloo1111.github.io/Currency-App/.../prices.json`): دلار/یورو/درهم/لیر، طلای ۱۸ و ۲۴، مثقال، سکه امامی/بهار/نیم/ربع/گرمی — JSON استاتیک روی GitHub Pages (CORS باز؛ مناسب مرورگر).
- ✅ **بیت‌پین** (`api.bitpin.org/api/v1/mkt/tickers/`): کل بازارهای تومانی/تتری + درصد تغییر ۲۴ ساعته — مبنای نرخ مشتق دلار (تتر).
- ✅ **Gold-API** (`api.gold-api.com`): اونس XAU/XAG/XPT (طلای/نقره/پلاتین). XCU (مس) وجود ندارد.
- ✅ **یاهو/COMEX** (`HG=F`): مس آتی به دلار بر پوند.
- ✅ **CoinGecko** و **er-api**: زنده (CoinGecko گاهی rate-limit می‌دهد — عامل «ثابت شدن» قبلی).
- ❌ **نوبیتکس** (`/market/stats` → HTTP 500) و **metals.live**: خارج شدند.
- ❌ `api.bitpin.ir/api/v2/...`: 404 — مسیر درست `api.bitpin.org/api/v1` کشف شد.

**خطاها و اصلاحات (کشف بزرگ):**
- 🔴 **کشف باگ نرمال‌سازی واحدها:** در ابتدا فرض شد همه قیمت‌های طلایار «تومان» است؛ با **تطبیق داخلی** (طلای ۱۸ عیار گرم ← اونس جهانی × نرخ دلار) مشخص شد فید **واحدهای مخلوط** دارد: سکه‌ها/ارزها/مثقال/انس به **ریال** ولی گرم طلای ۱۸/۲۴ به **تومان**. بدون این تطبیق، همه قیمت‌ها ۱۰ برابر خطا می‌خوردند. نرمال‌سازی `÷۱۰` در فاز ۹ پیاده شد (جدول کامل در `docs/PRICE_ENGINE.md`).
- تأیید واحدهای بیت‌پین: جفت‌های `_IRT` عملاً تومانی‌اند (با تطبیق USDT/BTC با دروازه).
- شناسایی عامل اصلی «قیمت ثابت»: وابستگی به دو منبع خارجیِ در ایران ناپایدار + نبود کش آخرین قیمت + نرخ رسمی er-api (به‌جای نرخ بازار).

---

## فاز ۹ — موتور قیمت چندمنبعه + دارایی‌های جدید ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- `src/lib/prices.ts` (**بازنویسی کامل**): موتور چندمنبعه موازی (طلایار/TGJU، بیت‌پین، CoinGecko، Gold-API، COMEX، er-api) + نرمال‌سازی ریال/تومان + قیمت‌های **مشتق** (نرخ دلار از تتر، طلای ۱۸ از اونس، آب‌شده از مثقال، نقره/پلاتین از اونس فلز، مس از COMEX) + **کش آخرین قیمت واقعی** (`jibino.prices.v1`) + آرایه `sources` (سلامت هر منبع).
- `src/lib/types.ts`: تایپ‌های `PriceSource` (منابع جدید)، `SourceHealth`، فیلد `unit` در `LivePrice`، طبقه جدید دارایی `metal`.
- ردیف‌های قیمتی جدید: **طلای آب‌شده (مثقال)**، **سکه امامی/بهار آزادی/نیم/ربع/گرمی**، **نقره (گرم)**، **پلاتین (گرم)**، **مس (کیلوگرم)**، یورو/درهم/لیر + ۵ رمزارز جدید (TRX/BNB/TON/LINK/AVAX).
- `src/pages/Assets.tsx`: پریست‌های جدید در فرم افزودن دارایی + طبقه «فلزات» + پنل «نرخ‌های مرجع» جدید با برچسب منبع هر قیمت و نوار سلامت منابع (✓/✕).
- `src/lib/analysis.ts`: تفکیک طبقه «فلزات» در ترکیب سبد.
- `src/lib/store.tsx` + `Layout.tsx`: **رفع باگ to-updates تایمر قیمت** (closure کهنه که با نرخ دستی تازه کار نمی‌کرد) — تایمر ۵ دقیقه + رفرش هنگام بازگشت به تب به store منتقل شد.
- `src/pages/Dashboard.tsx` / `Layout.tsx`: کارت‌های قیمت داشبورد و تیکر کناری شامل آب‌شده و سکه امامی شد.

**چرا:** طبق گزارش کاربر قیمت‌ها ثابت می‌ماندند و دارایی‌های بازار ایران (سکه/آب‌شده/فلزات) پوشش نداشتند.

**چگونه:** زنجیره انتخاب برای هر قیمت: `دستی ← منبع مستقیم ← مشتق ← کش ← ثابت آفلاین` — هر منبع مستقل fetch می‌شود و خرابی‌اش ایزوله است؛ برچسب هر قیمت در UI منبع برنده را نشان می‌دهد. فرمول‌های مشتق در `docs/PRICE_ENGINE.md` مستند شد.

**خطاها و اصلاحات:**
- ⚠️ **سه خطای کامپایل/lint حین نوشتن پیدا شد:** ۱) استفاده از `refreshPrices` در `useRef` قبل از تعریف (خطای TDZ) — با جابه‌جایی ارجاع ref به بعد از `useCallback` اصلاح شد؛ ۲) ترکیب `||` و `??` بدون پرانتز (خطای syntax) در درصد تغییر طلا — با پرانتزگذاری اصلاح شد؛ ۳) فراخوانی `bitpinToman` بدون محافظ وقتی منبع بیت‌پین خراب است (خطای احتمالی زمان اجرا) — با شرط `bpOk` اصلاح شد. همه پس از اصلاح با `tsc` سبز شدند.
- ⚠️ **سه خطای lint جدید** (دسترسی به ref در render ×۲ در store و متغیر بلااستفاده `i` در prices): فوراً اصلاح شدند (انتقال به `useEffect` و حذف متغیر).
- ⚠️ **خطای محیطی:** در یک اجرا `npx tsc` به‌جای TypeScript پروژه، پکیج قدیمی `tsc@2.0.4` را برداشت (چون `node_modules` بین نوبت‌ها پاک شده بود) و تأیید انجام نشده بود. **تشخیص:** پیام خود tsc. **اصلاح:** `npm ci` مجدد + اجرای `./node_modules/.bin/tsc` — تأیید واقعی سبز شد.
- ✅ اثر جانبی مثبت: بازنویسی prices.ts تایپ `any` قبلی را هم حذف کرد (lint: ۸ ← ۷ خطا؛ بدون خطای جدید).

---

## فاز ۱۰ — سند راهکار، تأیید نهایی و انتشار ✅

**تاریخ:** ۲۰۲۶-۰۹-۲۵ (۳ مهر ۱۴۰۵)

**چه چیزی:**
- `docs/PRICE_ENGINE.md`: سند کامل «راهکار قیمت» — معماری چندمنبعه، جدول منابع و endpointها، نرمال‌سازی واحدها، فرمول‌های مشتق دلار/ریال و فلزات، زنجیره fallback، کش و نقشه راه (سرور واسط Vercel، نرخ مشتق وزنی، تاریخچه قیمت، override هر نماد، هشدار قیمت).
- به‌روزرسانی همین CHANGELOG (فازهای ۸ تا ۱۰).

**نتایج تأیید نهایی:**
- `tsc -b` ✅ سبز | `npm run build` ✅ سبز | `eslint`: ۷ خطا (همگی قدیمی و مستندشده — بدون خطای جدید)

**چک‌لیست QA پیشنهادی:**
1. باز کردن اپ ← نرخ دلار/طلای ۱۸ باید از «طلایار/TGJU» یا «بیت‌پین» (برچسب مشخص) بیاید نه «آفلاین»؛ چند دقیقه بعد دکمه به‌روزرسانی، زمان تازه نشان دهد.
2. قطع اینترنت و بازکردن مجدد ← قیمت‌ها باید «کش» باشند (آخرین قیمت واقعی)، نه اعداد ثابت قدیمی.
3. افزودن دارایی ← پریست‌های «طلای آب‌شده، سکه امامی/بهار/نیم/ربع/گرمی، نقره، پلاتین، مس» باید با قیمت لحظه‌ای پیشنهاد شوند.
4. نوار سلامت منابع در صفحه دارایی‌ها ← وضعیت ✓/✕ هر منبع مشخص باشد.
5. نرخ دستی در تنظیمات ← باید بر همه منابع مقدم باشد و تایمر رفرش آن را رعایت کند.
